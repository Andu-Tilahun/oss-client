/**
 * Payment schedule types for the lease registration flow.
 * Canonical definitions live in `farm-lease.model.ts`; this file is a convenience barrel.
 */
export type {FarmLeasePaymentLine, LeaseDefineTermsRequest} from './farm-lease.model';
