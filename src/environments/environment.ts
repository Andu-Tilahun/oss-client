export const environment = {
  production: false,
  // Relative URL — ng serve proxies /api to BACKEND_URL (default http://localhost:8080).
  // Override backend: BACKEND_URL=http://192.168.x.x:8080 npm run host
  apiUrl: '/api',
  androidAppGithubRepo: 'daniel-mekuria/OSS-Farm-Android',
  contactRecipientEmail: 'ossethio@gmail.com',
};
